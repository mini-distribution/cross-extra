diff -Naur a/src/desktop/qml/main.qml b/src/desktop/qml/main.qml
--- a/src/desktop/qml/main.qml	2013-03-21 23:11:11.000000000 +0100
+++ b/src/desktop/qml/main.qml	2013-03-22 01:25:42.123130885 +0100
@@ -19,7 +19,7 @@
 
 Item {
     id: browserView
-    state: "normal"
+    state: "fullscreen"
 
     function focusUrlBar() {
         urlBar.textInput.forceActiveFocus()
@@ -210,8 +210,7 @@
         }
 
         var i;
-        for (i = 0; i < savedUrls.length; i++)
-            openTabWithUrl(savedUrls[i]);
+        state = "fullscreen"
         for (i = 0; i < commandLineUrls.length; i++)
             openTabWithUrl(commandLineUrls[i]);
     }
