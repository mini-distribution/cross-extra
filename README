Mini Distribution "extra" build files
-------------------------------------

This collection of build files include extra features. Build files in this
repository may only depend on buildfiles in core, opt, and extra.

The "extra" build files are loosely maintained on a non-regular basis.
